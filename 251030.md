JPA가 들어가는 곳이 레파지토리임
JPA의 목적은 엔티티와 테이블을 연결 :: 즉, 자바와 디비를 연결
mapping : 화면연결 / 다른언어(이기종) 연결
@require~ : 컨트롤러에서는 서비스와 연결

✔️무조건 다른 언어와 연결한다 = mapping
같은언어와 연결 = 생성자 (constructor) 

명시되지 않았는데 실행이 된다면 그것은 디폴트라고 한다
컬럼이 그런 역할이다
- 기본 키 맵핑은 : @Id
- 필드와 컬럼 맵핑은 : @Column

✔️JSON : 자바스크립트 오브젝 노테이션 (객체 표기)
공통점
데이터를 적는 방식 => 표기법,// 무언가를 표시하는 것
차이점
annotation은 (주석처럼 코드)/ 설명을 달다 / 그외 추가 설명 예-@
notation은 정보를 어떻게 적느냐(음악의 악보, 수학식)/ 표기, 형식같은 거

표기(음표 같은 느낌, 글자도 포함)와 표식(약간 sign느낌 표지판/ 상징이있어)

인터페이스는 객체가 아님 속성이 없음 = 서비스

클래스는 오브젝트 노테이션을 걸어야 객체가 되는 것임 => 
결국 컬 블래스 있고 그 안을 구성하는 뭔가가 있어야함
어노테이션을 안붙히면 맨 위에 빈객체가 돼
controller에서 @이걸 빼면 빈객체인거야

✔️JPA에서 사용하는 어노테이션들, 예를 들어:

[@Entity](https://x.com/@Entity)
[@Table](https://x.com/@Table)
[@Id](https://x.com/@Id
[@GeneratedValue](https://x.com/@GeneratedValue)
[@Column](https://x.com/@Column)
이거 jpa 규약야 자바언어로 작성히는가야
jpa(직원)는 규격, 표준일뿐 실제 일은 hibernate가 한다
=자바(언어)에서 interface와 서비스 임플리를 뜻해
=스프링(틀-직원관리 시스템 회사느낌)에서는 

스프링은 레파지토리를 관리해주는 도구임
레파지토리는 저장소 그 자체 디비의 데이터 주고 받음


✔️어플리케이션 컨텍스트가 최상위 부모야
그 내가 가지고 있는 데모어플리케이션 의미

html에서 연결할때 우리가 앞이랑 뒤에 디폴트로 붙혀준거 어플리케이션 프로퍼티
이게 컨텍스트임

뉴를 한다음에 어노테이션 이게 컨텍스트에 있다

스프링빈과 자바의 객체가 있다
스프링빈은 자바 객체 그 자체래 그것을 관리해주는 역할이어서
클래스 자체는 설계도인데 아무 기능이 없어 여기서 동작 가능한 상태가 되면
스프링빈이 관리해주나봐
클래스 자체는 저장소로 보다가 설계도로 보다가 ????

뉴를 어노테이션이라고 봐도 됌
자바에서 Controller controller = new controller :: 스프링에서 @Controller 임
이렇게 볼수도 잇어 어노테이션을 타입으로 보고 밑에 클래스명을 우리는 실현체로 볼수도 있게 되는거야
그러면 클래스명이 자식이 되고 어노테이션이 부모가 되는거야

List<>(이건 타입인데 interface역할) ls = new Arraylist 이건 앞뒤가 다른거임
이건 안없어져 
근데 앞뒤가 같으면 없어지는거야

serive ps = new service

앞에 타입이 부모가 되고 그것을 뒤에 자식한테 부여하면 되는데
예로 Object messenger = new Messenger 이렇게 해도 돼 부여한거야서

위에 있는 어노테이션은 이미 new가 있기 때문에 또 new를 하면 안된다

자바객체는 인스턴스 일회용임 // 스프링이 뉴로 한 것은 안없어진다(어노테이션) 빈객체는 컨테이너 밑에 있어 이건 자바객체가 아니야
근데 얘는 인스턴스가 아니어서 그래서 자바객체가 안와

컨텍스트를 이제는 스택틱 객체라고 함

어노테이션은 가비지컬렉터가 지울 수가 없어

도커와 스프링 연결을 위해 영원히 지워지지 않은 인프라를 구성해줄거

JPA도 hibernate가 일하게 뉴 때리는데 그것을 안지워지게 @이렇게 해줘
근데 이렇게 만들려고 하는게 스프링으로 하는거야

싱글존 즉 단 하나만 존재하는 객체를 만들어주는 것 = 어노테이션의 존재유무 하는일

어노테이션은 컨텍스트에 저장해라

✔️create  table하면 우리가 직접 만들어서 mapping이 안돼
이러면 스프링이 알아들을 수 없어
그렇기 때문에 어노테이션으로 직접 만들어라 라고 해야해


✔️html xml yaml zml
데이터 교환포멧)(json) , xml = rest ,, soap
soap는 복잡하고 무거워서 메모리 차지 심하니까 정말 필요한 거 아님 안갖게돼
근데 그만큼 안정적이긴 해// 근데 rest가 더 모바일 친화적이어서 현대적임


✔️ yaml은 도커에서 쓰는것
사진확인 251030 오전 11:54

create table 자동생성
실행 후 또 실행을 하면 에러가 뜨는데 update해줘야해


✔️숫ㅈ에서 문자로 변환하면 형변환이라고 하는데 이걸 캐스팅이라고 한데
문자에서 숫자로 변환하면 파싱이라고 함


✔️ ||은 sql에서 +임,  컬럼

✔️![[Pasted image 20251030130424.png]]
or은 혹시 스페이ㅣ스바 때렷을까봐 오류막기 위해 추가해준것

✔️pgAdmin에서 10번 문제 내 질문
스칼라에서 구한 값은 독립적인 값임 스칼라를 보면 괄호가 있는데 그게 자식이야
그래서 밖에 있는 팀네임은 부모야 즉 기존의 팀네임이 아닌 새로운 값을 생성된거야
그래서 마지막 줄에서도 이미 생성된 새로운 값에 대한 얘기를 하기 위해
order뒤에서 t.을 안붙혀준거야

서브쿼리는 연산의 목적이야 그래서 from에서 연산을 즉 인라인뷰나 서브쿼리를 넣어주면 연산을 하고나서 select으로 넘어가니까 더 느려지므로 스칼라에다가 넣어주는 것임.
만약에 서브쿼리를 넣어준다면 and로 해서 쿼리를 넣어주면 된다고 했음