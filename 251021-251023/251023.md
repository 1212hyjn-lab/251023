리소스와 자바 : 리소스에는 기능은 없어 객체가 아니다.(불완전)_기능x
리소스에서 자바로 올려주는 것을 로딩이라고 함

csv : 해당부분도 기능이 없어 _불완전
-가치있는 존재로 만들기 위해서 객체로 만들어 자바에 보내줌

뭘 개발해도 화면부터 시작(리소스->자바)
비정형데이터를 데이터로 만들기 위해 도커 다운 받았음

비정형데이터는 getmapping으로 불러온 것
컨트롤러에서 화면으로 다시 보낼때 ""이것을 붙혀줌
그러나 화면에서 보낼때는 ""이것을 보내지 않음
-> 

✔property와 attribute의 차이
from = 태그/ action"" = 액션어트리뷰트/ <>=어트리뷰트
이러고 나면 괄호안에 이메일이나 패스워드를 담아()서 
loginDTO(email) 이렇게
핸드폰 사진 참고
-> html에서는 attribute라고 해 
-> html에서 elements를 사용하는 걸로 봐서 html은 구조(복수)임
-> html은 기능이 없으니 ()이 존재할 수 없다
✔UserController에서 밑에 users.add가 있는데 이 add는 무엇인가
property가 set이고 attribute가 get이다.

✔파라미터 = 변수
인스턴스면서 로컬변수인 어떤 아이 위에 ()파라미터를 생성하게 되는데
파라미터는 경계선이 된다
✔args = argument
파라미터와 args를 구분하지 않는다
파라미터라고도 args가 되기도 하는 것임
![[Pasted image 20251023105503.png]]
![[Pasted image 20251023105510.png]]
![[Pasted image 20251023105526.png]]
맨밑에는 파라미터 나머지 두개는 args임
=둘다 담는 것은 맞으나 모양이 다르다//
              =
🎈넘어가는 모양을 봐야해
$의 의미는 html땅이라고 봐

✔int a=3;
     a=4;
    이렇게 되면 a=4로 나와
이것을 replace라고 부른다.

근데 add가 나오면 replace가 되는게 아니라 추가되는 것이다.
승객 5명에 대한 정보를 보기위해서 user뒤에 add를 붙힌 이유
=오버라이딩이 replace가 되고 오버로딩이 add가 된다
=객체지향 언어에서 디폴트는 오버로딩이다.

공학세계에서 존재하는 괄호의 갯수는 4개다.

[]배열 -> list구조
{}block
()메소드
//[][]행렬임 //[][][]3차원->[]차원으로 볼 수 있다 => 3차원은 인공지능임
//[]없으면 0차원이어서 없는 것도 기능이다.->a=3일때 0차원임

List<userDTO> users = new ArrayList<>();
리스트 자체는 []이건데 표시할 수가 없어 지금 저것은 userDTO는 1번째 줄에 있는 것을 가지고 온 것이어서 리스트와 합치면 2차원이 돼
그래서 사각형 안에서 이루어지는 것임 2차원안에 CSV파일이 들어갈 수 있음..
=엑셀은 sheet가 나눠져 있음 sheet한장을 csv라고 보면 돼
=map은 다차원임
=꺽새의 의미
=array 배열


📕오버라이딩은 oop의 4대 속성 중 다형성에 속한다
같은 이름의 메서드가 객체에 따라 다르게 동작한다.
4대 속성은 캡슐화(은닉화 : 콩깍지), -> DTO
          상속은 아직 안함
          추상화  
          다형성

- user.setEmbarked -> user은 인스턴스
- users.add(user); -> user은 엘리먼트
이때 user가 안에 있는데 이것은 파라미터 안으로 구분 = 경계선

<>태그안에 어트리뷰트는 많이 있어도 돼
비어있는 태그는 앞에 있는 것이 반복된 것
<?> 타입이 정리되지 않았다

클래스이름이 타입으로 변환이 되는데, html에 태그 안에 있는 div도 타입이된다.
태그안에 어떤 타입은 어트리뷰트 갖고 있고, 어떤 것은 갖고 있지 않다.

파이브라인 하나하나가 한 타입이 된다

게터와세터는 기능은 맞다. 
프로퍼티에는 게터세터가 있다    (로그인같은거 명확) -> DTO
어트리뷰트는 = 해서 넣으면 그만이다. (빠름)

✔user의 폴더는 속성과 기능을 가지고 있는데 도메인이 속성이되고 나머지가 기능이 된다, 따라서 폴더는 객체다.

속성의 타입을 주는게 타입이 되고, 기능에도 타입을 주게된다.
기능만 모아놓던 타입만 모아놓던 하나만 모아놓은 타입도 잇다.

부모자식관계가 여기서 출현한다.
컨트롤러와 레파지토리 그리고 서비스를 공통으로 만드는게 인터페이스다.

프로퍼티가 변한다는 의미고, 어트리뷰트는 변하지 않는다는 속성을 지님
어트리뷰트는 게터와세터를 안만들어주는게 변화하는 값이 여기서 나오거든


Calc에다가
서비스에 public interface로 바꿔서 기능을 갖게해줌
속성은 감추고 기능은 오픈한다

이게 추상메소드
![[Pasted image 20251023120906.png]]
