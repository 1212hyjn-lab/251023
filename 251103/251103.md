stream _ pipeline_ / instance (copy) : (disposable) / 
server : controller를 기준으로 서비스 제공하는 녀석
컨트롤러와 여러 개의 클라이언트을 연결 일대다 방식이라면 컨트롤러가 부모가 된다
부모는 먼저 존재해야해 / 클라이언트에는 부모에 대한 fk와 키를 갖는다.

(수요를 이기지 못한 타임리프는 인공지능 연결이 안돼)

컨트롤러를 인천공항으로 비유했다
레파지토리를 기준으로 쪼갠다면 컨틀롤러 기준으로 쪼개고 레퍼지토리를 반으로 쪼갠 그 사이를 우리는 서버라고 한다 (자세히 말하자면)

그렇다면 서버와 디비 중에는 누가 부모가 될까?
✔️디비가 부모다
즉 서버 쪽이 복제가 되니 자식이되고 인스턴스이다.
=본사인 넷플릭스가 내가 싱가포르에 가던 도쿄에 가던 나의 계정으로 시청이 가능하다면
넷플릭스는 디비가 되고 서버는 내가 이는 리전이 된다.

서버는 서버시를 제공하는 녀석임.// 
따라서 서버를 우리는 마이크로서비스라고 한다(micro)
그리고 디비 쪽을 macro -> 매크로시티라고 한다

ms-------------------repo-------------------------db ((대략 이런 모습))
repo 안에 entity는 하나 존재하게 된다

💎엔티티에
어노테이션을 입력하면
밑에 테이블이 생성되는데 어노테이션은 두개까지만 만든다.

💎컨트롤러
mapping은 이기종 언어 연결
컨트롤러에 있는 리퀘스트맵핑은 html로 연결된다 
즉 html에서 디비로까지 종점으로 까지 가게 되는 연결이다

리콰이어는 서비스로 갈거야임

💎💎엔티티
엔티티의 오류를 잡으려고 해도 다른게 import되기 때문에 에러
그래서 ㅡ그래들에 의존성을 넣어줘야함
✔️프롬포트는 사진으로 찍어뒀으니 참고할것


XX xx = new -> constructor // 그 뒤에는 ()인데 이것을 args
값을 넣고 싶은데 no이면 set으로 설정
all은 개체 수 만큼 넣어주면 돼// 얘는 set이 잇으니까 굳이 안넣어도돼

-  나중 추가는 no임 뭘 추가할지 모르니까
-  수정할 것이 없으면 all을 넣으면 돼

yml로 추천한 이유는 디비에서 서버를 여러개로 나눌 수 있어서
아침에 말했던 설명대로 yml을 사용하는 것을 추천함


ddl= 테이블 만드는 언어 -> 처음엔 create이고 나중에 update로 변경

![[Pasted image 20251103155854.png]]
새롭게 다운받은 이니셜라이즈

JPA로 테이블을 생성하면 인덱스 값을 알아서 생성해주기 때문에 더 빨라서
이렇게 생성함

 